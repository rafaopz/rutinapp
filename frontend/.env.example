# URL base del backend en PRODUCCIÓN (incluye el sufijo /api/v1).
# Ejemplo: https://rutinapp-api.onrender.com/api/v1
# En desarrollo déjalo vacío/sin definir: se usa el proxy de Vite a 127.0.0.1:8000.
VITE_API_BASE=
